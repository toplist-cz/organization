{
    "name": "profi-org",
    "version": "0.0.1",
    "devDependencies": {
      "sass": "latest"
    },
    "scripts": {
        "prepare": "sass --load-path assets/css --no-source-map --style compressed assets/css/style.scss assets/css/style.css"
    }
}
